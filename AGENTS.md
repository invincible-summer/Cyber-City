# AGENTS.md — 霓湾 / Neon Haven

> 项目规范版本：1.0 · 编写日期：2026-09-19  
> 放置位置：Godot 项目根目录，与 `project.godot` 同级。  
> 当前执行入口：`chapter1.md`。本文规定整个项目的方向、边界、架构和验收方式。

## 0. 给 Coding Agent 的首要指令

你正在持续开发“霓湾”：一个能在 Windows 本地离线运行、按独立地图逐张扩展的精美 3D 城市场景项目。当前产品形态是**可自由观察和摄影的城市环境展示程序**。后续可以扩展为短剧布景库和导演工具。

当前优先级依次为：

1. 工程可以打开、运行，资源路径正确，地图加载和卸载可靠。
2. 默认运行负担低，帧率受控，不因地图数量增加而同时激活更多场景。
3. 第一张地图达到统一、细致、有构图的成品场景质量。
4. 建筑、材质、地图接口可以复用，后续逐张增加地图。
5. 为将来的摄影和角色表演提供少量明确接口。

用户后续明确指令可以调整本规范。`chapter1.md` 是第一阶段工作单，不授权实施本文全部远期路线。发现两份文档矛盾时，先依照最新用户要求，其次依照本文硬性约束；在交接记录中说明具体矛盾。

对边界清楚、可逆的实现选择自行推进。缺少 CPU、内存、显卡功耗等信息时使用本文初始预算并记录假设，不反复要求用户选技术细节。必要资源确实不可用时，交付已经完成的部分和明确的验证缺口，不把未运行的功能标成通过。

**每次开始执行任务前，都要根据用户要求仔细阅读详细思考对应的 chapter?.md 的文件或 plan.md 文件，并制作详细计划再开始执行，且执行过程中每次完成一个小阶段开始下一步前都要重新阅读 chapter?.md 的文件或 plan.md 计划文件对应部分明确语义和要求防止落实误差.**

**AGENTS.md 和其他文档在必要的时候需要更新，但请注意，更新的时候要清理掉过期的旧语义和旧记录，相关文档的更新不是无限制增加章节和字数，而是动态维护时效性.**

**注意我们要求作品精美的前提下需要让其项目运行时对电脑低负荷、轻量化，且保持项目质量**

## 1. 已知条件与范围

### 1.1 用户环境

- 用户认为整机性能较差，明确要求运行时负担低。不能仅凭显卡名称假定高功耗模式、充足内存或持续满载可接受。
- 用户已持有 `Godot_v4.7.2-stable_win64.exe`，目标使用 **Godot 4.7.2 标准版**。
- 编程 Agent 每日约有 6 小时可用预算。采用可中断、可续做的任务队列；该额度不等于完成时间保证。
- 目标平台首先是 Windows x86_64 桌面端；运行不依赖 Python、Blender、网络服务或 LLM。
- 工具路径："D:\Godot\Godot_v4.7.2-stable_win64.exe\Godot_v4.7.2-stable_win64.exe"
- 我提供了 Godot mcp，优先使用它。

Godot 官方存在 [4.7.2 stable 发行页面](https://godotengine.org/download/archive/4.7.2-stable/)。实施时仍须执行本机 `--version`，确认二进制的实际版本；不得仅依据文件名判断。

### 1.2 当前必须包含

- 真正的 3D 建筑和街道，有完整的近景、中景、远景层次。
- 一个轻量应用外壳，以及严格单地图活动的地图管理器。
- 第一张完整可观赏地图；以后每张地图作为独立内容单元加入。
- 观察摄影机、固定观景机位、画质档位、显示/隐藏 UI、截图。
- 低成本的少量环境动画；支持全部关闭。
- 地图/资源生命周期检查、可重复的性能路线和实际画面截图。

### 1.3 当前不实施

- NPC、居民模拟、对话、任务、战斗、背包、经济、车辆 AI、寻路。
- 玩家角色、人物骨骼动画、物理行走控制器、交互物体、可开门窗。
- 联机、账号、数据库、联网生成内容、运行时调用 LLM。
- 无缝大世界、全城后台模拟、无限程序生成、地图自动流式预取。
- 写实级动态光照、光线追踪、实时镜面、多层全屏特效。
- 完整导演时间轴、演员系统、配音系统；这些在后期单独实施。

这里“暂时无互动”指没有游戏玩法和场景对象交互。观察摄影机、地图选择、截图、设置等工具操作属于当前允许范围。

## 2. 产品定义与主题

项目中文名：**霓湾**。英文名：**Neon Haven**。仓库/目录建议名：`neon_haven`。

一句话定义：**一座温暖、安静、带有复古科技气息的近未来滨海城市，以独立街区组成可持续扩建的 3D 场景库。**

世界背景：旧城区保留了狭窄商铺、阳台、管线和修理铺；近未来交通设施、智能终端和通信设备被逐步加入原有城市。生活痕迹来自建筑和物件布置，当前无需人物系统支撑。

第一张地图的主题是“雨停后的蓝调时刻”。天空仍有余光，店内暖灯已经亮起，远处高架线和信号塔形成轮廓。地面只保留少量潮湿区域；不持续下雨，不做地面积水模拟。

后续地图共享城市身份、招牌语言、建筑尺度与材质库，各自有独立主题和光照。未来短剧可以在这里拍摄日常、悬疑、科幻和温和喜剧。

## 3. 美术规范

### 3.1 风格定位

采用**风格化写实比例、适度低多边形、精细表面处理、复古未来主义**。

- 建筑接近真实比例，轮廓简洁；近处的门窗、雨棚、檐口和重要家具需要倒角与厚度。
- 表面质感来自粗糙度变化、法线、接缝、受控污渍和烘焙明暗。
- 模块化重复必须经过高度、开间、窗型、招牌、颜色、退台和附属设施变化。
- 霓虹只作为视觉重点，发光区域约占构图的小部分；不要让全城所有轮廓同时发亮。
- 不追求强制三角面外观，不使用大面积毫无细节的纯色立方体充当最终建筑。
- 不依靠过暗曝光、过强雾、过量 Bloom 遮盖未完成部分。

### 3.2 基础色板

| 用途 | 建议色值 | 说明 |
| --- | --- | --- |
| 深蓝灰 | `#263445` | 阴影与深色金属 |
| 蓝灰墙面 | `#607789` | 主体建筑冷色 |
| 暖灰混凝土 | `#A59D90` | 平衡冷暖、形成材质差异 |
| 窗内暖光 | `#F2B66D` | 店铺与生活气息 |
| 青色标识 | `#58C4C1` | 少量科技元素 |
| 橙红点缀 | `#DF735A` | 地标、导视和小面积焦点 |

颜色表指导材质搭配，不要求最终屏幕像素等于十六进制值；光照和色调映射会改变显示结果。普通材质保持合理亮度，发光材质单独管理强度。

### 3.3 构图和尺度

- 单位统一为 1 Godot 单位 = 1 米，Y 向上，摄影机默认朝 -Z；模块原点、接缝方向一致。
- 常规楼层约 3.0–3.6 米，门高约 2.1–2.4 米，街道尺度与车辆、座椅相互协调。
- 每张地图必须包含：一个主要地标、三个摄影构图、一个安静留白区域。
- 至少一幅人眼高度画面和一幅高位全景。禁止所有宣传图都来自看不到缺陷的远距离俯视。
- 场景背景使用本地图拥有的简化建筑/天际线；不能实例化另一张真实地图充当远景。
- 摄影机允许到达的区域必须有完整可见面。未制作的建筑背面不得落入可用观景路线。

### 3.4 动态氛围

第一阶段只允许少量招牌亮度变化、通风扇缓慢旋转、局部蒸汽等。不得全场景每个物件挂逐帧脚本。第一张地图最多 6 个非摄影机持续动画节点、2 个局部粒子发射器；经济档粒子默认关闭。

画面没有人物也应成立。通过摆放椅子、修理工具、招贴、窗帘、外卖箱等静态物件表现生活痕迹。

## 4. 技术栈与依赖

| 层级 | 选择 | 使用边界 |
| --- | --- | --- |
| 引擎 | Godot 4.7.2 标准版 | 固定版本，Windows x86_64 |
| 游戏脚本 | 带类型标注的 GDScript | 摄影机、地图生命周期、设置、工具 UI |
| 默认渲染器 | Mobile，优先 Vulkan | 适用于当前静态、烘焙为主的场景 |
| 备用渲染路线 | Compatibility/OpenGL | 需要单独验证的启动选项，不自动承诺同等画面 |
| 场景/配置 | `.tscn`、`.tres`、小型 JSON 注册表 | 地图主体路径使用字符串，不预加载所有地图 |
| 美术资源 | glTF 2.0 / `.glb`、PNG/WebP、Godot 材质 | 最终可离线打开 |
| 可选制作工具 | Blender、离线 Python 脚本 | 只在制作阶段运行，版本与用途写入工具文档 |
| 静态光照 | LightmapGI | 每张地图独立烘焙 |
| 版本管理 | Git | 大型源资产达到需要时才引入 Git LFS |
| 测试 | 小型 Godot 测试场景与脚本 | 聚焦加载/卸载、错误处理、设置和证据采集 |
| 后续视频 | Godot Movie Maker；FFmpeg 可作为离线转换工具 | 不把 FFmpeg 设为当前运行依赖 |

当前不引入 .NET/C#、自研引擎、Rust、GDExtension、ECS 框架、外部资产商店插件。只有性能测量确认现有方案成为瓶颈时，才讨论增加原生代码。

Godot 的 Mobile 渲染器也可用于桌面端。本项目选择它是基于静态场景和特效需求的初始工程决策，不宣称它在所有设备上都比其他渲染器快。参考：[渲染器比较](https://docs.godotengine.org/en/stable/tutorials/rendering/renderers.html)。

渲染器与画质档位是两个不同概念：经济/均衡档在同一 Mobile 渲染器里切换。切换渲染器可能需要重启和材质/光照调整，不实现伪装成即时画质选项的渲染器切换。

## 5. 场景架构与目录

项目外壳长驻，地图内容按需进入 `MapSlot`。外壳仅保留相机、工具 UI、设置和地图管理服务。

| 路径 | 职责 |
| --- | --- |
| `project.godot` | 项目设置与主场景 |
| `AGENTS.md` / `chapter1.md` | 长期规范 / 第一阶段任务 |
| `scenes/app/main.tscn` | 主场景：MapSlot、CameraRig、工具 UI |
| `scripts/app/` | 启动、设置、摄影控制、截图 |
| `scripts/maps/` | MapManager、MapDefinition、地图公共脚本 |
| `scripts/diagnostics/` | 可关闭诊断、性能采样 |
| `data/map_registry.json` | 轻量地图注册表 |
| `data/quality/` | 经济档、均衡档配置 |
| `maps/m01_afterglow/` | 第一张地图的场景、定义、构图锚点、烘焙资源 |
| `assets/shared/` | 经至少两个实际使用场景确认可复用的资产 |
| `assets/m01_afterglow/` | 第一张地图独有资产 |
| `tools/` | 离线资产/场景制作脚本与使用说明 |
| `tests/` | 生命周期、配置、错误路径与图形验证入口 |
| `docs/` | 环境记录、待办、决策、交接、验证说明 |
| `artifacts/` | 本轮截图和性能报告；按体积管理，不随运行包导出 |
| `build/` | Windows 导出产物；不提交可重新生成的大文件 |

第一阶段不要为尚未存在的功能创建大批空目录或空框架。上表中的远期目录在有真实内容时建立即可。

地图根节点必须继承/使用统一的 `MapRoot` 脚本并加入 `active_map_root` 组。第一张地图采用以下明确层级，所有需要烘焙的静态网格均在 LightmapGI 子树内；以实际烘焙结果验证没有遗漏。

| MapRoot 下的相对路径 | 节点类型 / 职责 |
| --- | --- |
| `BakedWorld` | LightmapGI，覆盖本地图需要烘焙的静态内容 |
| `BakedWorld/StaticGeometry` | Node3D，建筑与地面网格 |
| `BakedWorld/Props` | Node3D，需要烘焙的静态道具 |
| `BakedWorld/Lighting` | Node3D，参与静态照明的灯光 |
| `Backdrop` | Node3D，不依赖本地图 lightmap 的简化远景 |
| `Environment` | WorldEnvironment |
| `CameraAnchors` | Node3D，三个 Marker3D 构图锚点 |
| `Ambient` | Node3D，少量可关闭环境动画/粒子 |
| `Reserved/ActorRoot` | 空 Node3D，未来演员容器 |

参与光照的小道具若改用 MultiMesh，先验证其烘焙限制；无法保证正确结果时保留正常网格实例。

一张活动地图只拥有一个有效 `WorldEnvironment`。摄影机由主场景拥有，地图只提供 Marker3D 锚点，不再创建第二个活动摄影机。

## 6. 地图合同

### 6.1 MapDefinition

使用轻量自定义 Resource 保存以下字段，精确类型可在实现中补充，但语义不得改变。

| 字段 | 语义 |
| --- | --- |
| `schema_version` | 格式版本，初始为 1 |
| `map_id` | 稳定唯一标识，例如 `m01_afterglow` |
| `display_name` | 用户可见地图名称 |
| `scene_path` | 地图 `.tscn` 的字符串路径 |
| `preview_path` | 可选小尺寸预览图字符串路径，不持有地图资源 |
| `default_anchor` | 默认观景 Marker3D 名称 |
| `camera_bounds` | 本地图允许摄影机移动的 AABB |
| `camera_exclusion_bounds` | 少量建筑禁入 AABB；防止自由镜头进入未制作的楼体内部 |
| `anchor_names` | 已实现固定机位名称列表 |
| `lighting_profile_id` | 本地图已烘焙的固定光照版本 |
| `available` | 内容是否真实可用 |

注册表只保存地图 ID 和 MapDefinition 路径字符串。MapDefinition 不得持有 PackedScene、地图材质、LightmapGIData 或完整地图缩略场景。

未制作地图可以写在路线文档，第一阶段不把失效路径写入可选择的生产注册表。

### 6.2 生命周期接口

- `request_map(map_id)`：通过 ID 请求切换。只允许一个切换事务进行；UI 在切换中禁用重复操作。
- `unload_current_map()`：冻结地图输入/动画，清理地图引用，移除并释放旧根节点。
- `map_loading(map_id, progress)`：发布当前加载状态；进度仅指资源加载，不能冒充所有 GPU 准备工作都已完成。
- `map_loaded(map_id)`：实例化、环境配置、摄影机锚点和质量档应用完成后触发。
- `map_failed(map_id, reason)`：失败时回到可操作的空地图选择界面，显示可读错误。

### 6.3 状态机与切换顺序

状态集合：`EMPTY`、`READY`、`UNLOADING`、`LOADING`、`ACTIVATING`、`ERROR`。

合法主流程：READY → UNLOADING → LOADING → ACTIVATING → READY；从 EMPTY 初次进入时可直接进入 LOADING。卸载前的参数验证失败保持原状态；卸载后的失败进入 ERROR，清理任何部分实例后回到 EMPTY，再允许重试。

切换的强制顺序：

1. 验证 ID 和目标路径；无效请求保持原地图和设置不变。
2. 用不含 3D 场景的加载遮罩遮住画面，暂停观景控制。
3. 停止旧地图音频、动画、Tween、Timer；断开外壳对旧地图信号和节点的引用。
4. 从场景树移除旧地图，调用 `queue_free()`，等待删除生效。清空持有的旧 PackedScene、地图专有资源和回调引用。
5. 在旧地图卸载完成之后，才发起目标地图的后台资源加载。不得为了无缝效果同时预热两张地图。
6. 轮询 `ResourceLoader.load_threaded_get_status()`；只在已加载状态调用 `load_threaded_get()`。
7. 在主线程实例化并添加目标地图；不得在工作线程操作活动场景树。
8. 校验地图根、环境和锚点，应用质量档位，进行受控的首次显示准备，然后解除遮罩。

背景加载并不保证实例化、着色器准备或首次反射探针更新完全无卡顿。允许在加载界面完成这些工作，并分别记录资源加载与激活耗时。参考：[后台加载](https://docs.godotengine.org/en/stable/tutorials/io/background_loading.html)。

### 6.4 资源释放硬性约束

- 稳态时 `active_map_root` 数量恰好为 1；切换或菜单状态可以为 0；任何时刻不能为 2。
- 不得先实例化所有地图再设置 `visible = false`。
- 不得用 `process_mode = DISABLED` 冒充地图卸载；节点和资源仍占内存。
- 不得在 Autoload、常量、MapDefinition 中 `preload` 所有地图。
- 不保留地图专属材质、网格、烘焙数据和音频的无用途强引用；共享资产可合理缓存。
- 地图移除以后，不得留下其碰撞、音频、计时器、世界环境或逐帧脚本。
- `queue_free()` 不等于 GPU/驱动缓存立即归零。通过对象引用、节点计数、内存趋稳共同判断泄漏，不要求进程每次回到完全相同字节数。
- 摄影机切换时持有锚点的 Transform3D 值，不长期持有旧 Marker3D 引用。

## 7. 地图内的优化方法

当前以整张地图作为加载单位，不实现复杂的地图内资源流式系统。单张地图内部按约 20–40 米建筑/街段组织几何分组，以便遮挡剔除、LOD 和后期优化。

- 不把整个街区合并成一个无法有效剔除的巨型网格。
- 不把所有窗户放进覆盖全地图的同一个 MultiMesh；按立面或街段分组。
- MultiMesh 用于确实重复的装饰，不能假定每个实例能获得独立正确的 lightmap。需要独立烘焙明暗的建筑模块使用可正确烘焙的 MeshInstance3D，或在制作阶段按合理分组合并。
- MultiMesh 的当前实现与烘焙限制先做小样验证；其批次整体可见性会影响剔除效果。参考：[MultiMesh 优化](https://docs.godotengine.org/en/stable/tutorials/performance/using_multimesh.html)。
- 建筑墙体使用简单 OccluderInstance3D；不要把细小道具全部做遮挡体。遮挡剔除也使用 CPU，必须做开/关对照再确定收益。参考：[遮挡剔除](https://docs.godotengine.org/en/stable/tutorials/3d/occlusion_culling.html)。
- 远景使用低复杂度几何，轮廓连续；小道具通过可见距离减少成本，避免在固定摄影机画面中央明显跳变。
- 当前不需要导航网格和碰撞体。观察镜头采用受限自由飞行，以整体边界和少量建筑禁入 AABB 限制移动；摄影路线自行避开建筑。仅在相机移动时检查这些简单边界，不遍历场景网格。
- 资源生成、UV 展开、字体图集制作、网格合并均发生在制作阶段；发布程序启动时直接加载最终资源。

## 8. 光照、材质和资源制作

### 8.1 固定时间与烘焙

第一张地图只有一个固定蓝调傍晚光照版本。使用 LightmapGI 保存静态照明；光源位置、建筑位置或发光贡献改变后，需要重新烘焙受影响场景。

- 静态建筑准备无重叠 UV2，并设置合理的 lightmap 密度；保留需要版本控制的 unwrap 缓存和原始资源。
- 烘焙只在制作阶段执行，运行时不得自动烘焙。
- 静态灯优先使用静态烘焙方式；确认烘焙后不会继续保留重复的实时光照开销。
- 灯牌的发光材质不自动等价于能照亮整条街的实时光源；发光贡献和照明通过烘焙明确制作。
- 当前不做动态昼夜。将来需要另一时段时，以另一套经验证的光照版本/场景变体实现，单纯旋转太阳不能更新静态 lightmap。
- LightmapGI 和材质/天空反射来源各司其职；烘焙照明不是实时反射。

参考：[LightmapGI 工作流](https://docs.godotengine.org/en/stable/tutorials/3d/global_illumination/using_lightmap_gi.html)。文档所述低运行成本不表示 lightmap 没有显存、磁盘和加载成本；纳入预算。

### 8.2 反射与氛围

- 默认使用天空环境反射；均衡档允许最多 2 个低分辨率局部 ReflectionProbe，更新方式为 Once。
- Once 仍可能在首次进入/移动时产生更新成本，不称为零成本离线反射。地图激活时覆盖首次更新，稳态不移动探针。
- 不启用实时平面反射、SSR、SDFGI、VoxelGI、体积雾、SSIL；Mobile 下不依赖这些功能。
- 不依赖 SSAO 或景深等 Mobile 不支持的效果建立核心画面。使用烘焙接触明暗、构图和材质获得层次。
- 地面粗糙度使用区域变化表现雨后，不制作全街镜面。玻璃大多使用有色不透明/近似窗面，仅关键橱窗少量透明。
- 第一阶段最多 2 个额外实时无阴影局部灯；经济档关闭。当前没有动态人物，因此不需要动态阴影作为默认功能。
- 摄影模式不偷偷切换 Forward+；后续如需该路线，必须作为单独验证的扩展。

参考：[反射探针](https://docs.godotengine.org/en/stable/tutorials/3d/global_illumination/reflection_probes.html)、[渲染功能表](https://docs.godotengine.org/en/stable/tutorials/rendering/renderers.html)。

### 8.3 资产要求

- 第一阶段以原创建筑模块、离线生成几何和明确授权的少量基础素材为主；不能让购买大型资产包成为启动前置条件。
- 外部素材记录来源 URL、作者、许可和用途，保存到 `docs/asset_sources.md`。字体与音频同样记录。
- 自动化生成的模块需有尺寸、倒角、UV、法线和材质检查；参数变化要受美术规范控制。
- 常规纹理采用 512/1024，关键近景可用 2048；不默认引入 4K/8K 纹理。
- 重复材料共享；只有确实不同的参数才复制材质。贴图使用合适的 mipmaps 和导入压缩。
- 模型优先交付可直接导入的 `.glb`；`.blend` 可以保留为制作源文件，但用户打开 Godot 项目不得依赖本机安装 Blender。
- 本地开发工具可用 Python，发布程序不能调用 Python 生成整座城市。
- 如制作机器没有 Blender，优先用 Godot 离线脚本制作并保存模块；同样完成烘焙所需 UV2 验证，不降低最终质量要求。
- 生成脚本只覆盖明确标识的生成文件；手工精修文件单独存放，不能在下一次生成时丢失。

## 9. 初始性能预算

以下均为**设计目标和调查阈值，尚未在用户电脑实测**。记录实际 CPU、内存、GPU 型号、驱动、供电状态和版本后才能判断达标。不能把数值写成已经达到的性能。

### 9.1 运行质量档位

| 项目 | 经济档 Eco（默认） | 均衡档 Balanced |
| --- | --- | --- |
| 输出窗口 | 1920×1080，可调整 | 1920×1080，可调整 |
| 3D 内部分辨率比例 | 0.80，即默认窗口下约 1536×864 | 1.00 |
| 默认帧率上限 | 30 FPS | 60 FPS |
| 抗锯齿 | FXAA；若效果不佳允许关闭并记录 | MSAA 2×，不叠加其他 AA |
| 主要照明 | 同一套静态烘焙 | 同一套静态烘焙 |
| 额外实时局部灯 | 0 | 最多 2，无实时阴影 |
| 局部 ReflectionProbe | 关闭，以天空反射替代 | 最多 2，Once |
| Glow | 关闭或经测量后的最低必要强度 | 低强度，保持招牌可读 |
| 环境粒子 | 关闭 | 最多 2 个局部发射器 |
| 小道具可见距离 | 较短，保留三个主构图 | 正常 |
| 失去焦点 | 限制为 10 FPS | 限制为 10 FPS |
| 最小化 | 暂停环境动画，限制为 5 FPS | 同左 |

这些配置以当前引擎实际支持的属性实现。UI 保持原始输出分辨率，只降低 3D 渲染比例。不得给 Mobile 配置不存在的 FSR2 功能。VSync 与帧率上限需要组合验证，避免不同刷新率下异常抖动。

### 9.2 第一张地图的资源预算

| 指标 | 初始目标 / 阈值 | 测量说明 |
| --- | --- | --- |
| 活动地图数 | 稳态 1，过渡 0，不得为 2 | 场景树与生命周期日志 |
| 可见几何 | 常规机位约 ≤300,000 三角形，最复杂机位 ≤500,000 | 明确统计口径；引擎 primitives 不能无条件当作三角形 |
| Draw calls | 常规机位 ≤250，最复杂机位 ≤450 | 包括所测视图实际渲染成本 |
| 活动地图节点 | 约 ≤2,500 | 用于发现模块/细节节点过度膨胀 |
| 导出程序工作集 | 第一张地图稳态目标 ≤2 GiB | Windows 进程指标；不以 Godot MEMORY_STATIC 代替 |
| GPU 资源占用 | 初始目标 ≤1.5 GiB | 注明引擎估计或操作系统统计，不能混用 |
| 地图 lightmap GPU 占用 | 初始分配目标 ≤256 MiB | 按实际格式、层数、mipmap 计算/测量 |
| 暖启动地图加载 | 初始目标 ≤5 秒 | 不包括首次工程导入；冷启动另记 |
| 首张地图内容体积 | 尽量 ≤500 MiB | 不含编辑器、制作源文件与测试录屏 |

这些预算服务于相同目标场景的迭代，不是跨硬件通用最低配置。超过阈值先定位原因；不得直接删除所有装饰或让地图变黑来通过数字检查。确有必要调整预算时，附截图和测量原因，并在交接中明确。

## 10. 性能验证协议

实施阶段至少记录两种运行方式：用户默认的 Godot 工程运行，以及 Windows Release 导出运行。最终性能结论优先使用 Release；没有导出模板或目标设备时明确标为未测。

1. 固定 1920×1080 窗口、同一质量档、同一机位顺序、相同驱动/供电状态。
2. 首轮单独记录启动、资源载入、激活、初次渲染；随后预热 15 秒。
3. 使用固定的 60 秒无输入镜头路线，连续测 3 次。
4. 输出平均 FPS、帧间隔 P50/P95/P99、卡顿数量、Draw calls、节点数、相关内存指标。
5. 帧间隔包含帧率限制和等待，不能冒充 CPU 或 GPU 执行耗时。使用实际 profiler 获取 CPU/GPU 耗时；不能获取则填 N/A。
6. 经济档稳态目标：平均 FPS ≥29，P95 帧间隔 ≤40 ms，预热后无重复出现的 >100 ms 卡顿。
7. 均衡档稳态目标：平均 FPS ≥58，P95 帧间隔 ≤20 ms。未达到时报告真实结果，不临时改测试机位避开复杂区域。
8. 若检查剩余性能空间，可单独进行短时无上限测试，结束后恢复帧率上限；不可将其作为默认运行配置。
9. GPU 利用率、整机风扇和功耗受硬件/驱动影响。测不到就不编造“低于多少瓦/百分比”的结论。

地图生命周期测试：在正式地图与极小测试地图之间往返 10 次。两张地图从来不同时活动；卸载后的弱引用应失效。先预热缓存，再观察后 5 次是否持续增长；稳态工作集相对预热基准增长超过 10% 或 150 MiB 时开展调查。驱动正常缓存增长不直接判为泄漏，必须结合引用/对象证据解释。

测试地图仅存在于 `tests/fixtures/`，不列入用户地图菜单。它用于验证真正的 A→B→A，不能以同一地图改个名称代替。

## 11. 第一张地图概念与后续地图

### 11.1 M01：余晖街区 / `m01_afterglow`

- 可制作核心范围约 120×160 米；允许用较轻的外围轮廓扩展视觉纵深。
- 摄影机活动范围约 X ±50 米、Z ±70 米、Y 1.5–24 米；由 MapDefinition 统一配置。
- 内容：一条主街、一条侧巷、修理铺门前广场、街尾高架站入口、简化远景塔楼。
- 三个主构图：主街纵深、暖光修理铺、街尾站口与天际线。
- 约 12–18 个主要建筑体量，用 6–8 套立面语汇组合；其中至少 3 栋为明显不同的主建筑。
- 店铺只制作可见的浅景橱窗/室内盒，不实现可进入房间。
- 地面有材质分区、路沿、排水、标线和少量雨后粗糙度变化。
- 不要求第一阶段覆盖全部核心边界，边界外要通过正确背景和限制摄影路线保持完整观感。

### 11.2 后续内容方向（未获当前阶段实施授权）

| 顺序 | 地图 | 视觉主题 | 可复用内容 |
| --- | --- | --- | --- |
| M02 | 空中步道 | 蓝天、玻璃连桥、屋顶绿化 | 建筑模块、标识、相机、地图管理器 |
| M03 | 旧站广场 | 铁路设施、暖色清晨、工业细节 | 高架设施、地面材质、灯具 |
| M04 | 河岸夜市 | 水岸、摊位、远处城市剪影 | 商铺、招牌、模块化道具 |

未来每张地图加入前先定义唯一 ID、主题、预算、三个构图、可见边界和卸载检查；不修改旧地图的稳定 ID。

## 12. 摄影与短剧扩展合同

当前实现：固定机位、自由观察、隐藏 UI、截图、固定镜头路线作为验收入口。

当前只预留：稳定地图 ID、Marker3D 命名、一个空 `Reserved/ActorRoot` 容器、将来序列资源可以引用的机位与地标标识。空容器不附带更新脚本。

后续顺序：

1. 摄影机关键帧、速度曲线、镜头切换和镜头预设保存。
2. 时间轴驱动的角色走位、动作、字幕与音轨。
3. 表演状态快照与重拍；关键动作/事件明确记录，不仅保存随机种子。
4. Movie Maker 离线输出视频/图像序列，配套转码说明。

离线录制可以比实时更慢完成一帧，从而维持输出视频的固定节奏；它不能消除显存/内存需求，也不能让硬件支持原本不支持的渲染特性。参考：[Godot 视频制作](https://docs.godotengine.org/en/stable/tutorials/animation/creating_movies.html)。

当前不要为了远期短剧引入人物骨骼、完整动画状态机、音频时间线或复杂插件。

## 13. 工程规范

- 文件/目录用英文小写 `snake_case`；类名用 `PascalCase`；UI 与项目文档可用中文。
- 脚本优先带类型标注；职责清楚，避免一个巨大脚本同时负责建城、相机、UI、加载和测试。
- 优先使用 Godot 节点与资源组合，不为几个对象引入复杂继承/依赖注入框架。
- 可调参数进入质量配置、MapDefinition 或导出属性；不散落几十处魔法数字。
- 静态网格不附带无用途脚本；控制器只在活动时启用处理，诊断面板最多 4 Hz 更新显示。
- 地图加载是异步事务；处理失败、重复请求和窗口关闭，不永久卡在遮罩。
- 共享 Material/Resource 修改前判断影响范围；质量设置不得意外永久改写资产文件。
- 中文招牌保留可复用字体/字形资源和明确许可，不能输出乱码或缺字方框。
- 使用原生工具 UI：地图菜单、质量选择、机位、帮助。画面主次以城市为主，F1 可隐藏全部展示 UI。
- 默认不播放背景音乐。后续少量环境音随地图释放，音量可关闭。
- 生成资产与手工资产分别标识，脚本执行应可重复，不每次生成不同城市。
- Git 忽略 `.godot/` 缓存、导出构建和大体积录屏；保留源场景、需要的 UID、导入配置、最终资产与必要烘焙文件。不要误删 `.uid` 或 lightmap 数据。
- 外部资产、制作缓存和项目导出规则要分清。任何用户运行所需资产不能仅存在于制作工具临时目录。

## 14. Agent 每轮工作流程

1. 读取本文件、当前章节和 `docs/handoff.md`，检查 Git 状态与用户现有更改。
2. 从 `docs/backlog.md` 选择一个可验收任务；同一时刻只推进一个主要目标。
3. 写清本轮完成条件，然后实现。未经当前章节要求，不提前扩展 NPC、第二张正式地图或联网功能。
4. 涉及 Godot API/项目设置，先用本机 4.7.2 文档、已有工程或最小样例确认属性；不要臆造配置键。
5. 完成对应验证：代码逻辑用测试，场景质量用实际引擎截图，性能用可复现测量。
6. 若有 Git，在不覆盖用户更改的前提下建立小而完整的本地提交；未经用户要求不推送或发布。
7. 更新 `docs/handoff.md`：已完成、已验证、未验证、当前问题、下一项任务及精确运行命令。
8. 保留可打开的工程。接近时间预算时先收尾和记录，不为了填满 6 小时开始无法完成的大改动。

Agent 可以编写和运行离线美术制作脚本，但必须实际查看结果。脚本成功退出、节点数量很多、文件很大都不是精美场景的验收证据。

## 15. 验证与完成定义

### 15.1 工程验证

- 从干净目录导入后主场景可启动，没有缺失资源或 GDScript 解析错误。
- 无需安装 Python/Blender 即可用指定 Godot 打开最终工程。
- 有导出模板时，Windows Release 导出程序能从包含中文和空格的路径运行。
- 经济档默认启用；用户设置可保存并在重启后恢复，非法配置回落至默认值。
- 只制作一个生产地图。测试地图不能出现在发布地图列表。

### 15.2 画面验证

- 三个主构图都有 1920×1080 实际引擎截图；至少保留经济、均衡各一组对照。
- 近处建筑具备门窗进深、边缘/接缝、结构层次和材质差异。
- 地面有铺装逻辑和接触明暗；无明显悬空、闪烁、穿插、严重 UV 拉伸和漏光。
- 招牌可读、字体完整、光照层次清楚；不存在曝光过强的整片白色霓虹。
- 所有允许到达的相机机位都没有明显看到地图背面、世界空洞或近景被错误裁剪。
- 经济档保留场景的主题、地标和主要构图；不能退化为灰盒场景。

### 15.3 测试边界

Headless 可验证资源导入、脚本和场景生命周期；不能证明光照、GPU 性能或最终观感。没有可用图形环境时，图形项目必须标记“待图形环境验证”，给出可直接运行的检查入口。

测试不追求大量覆盖率数字。优先检查：单地图生命周期、无效地图处理、重复请求处理、质量档恢复、资源路径可解析、相机边界与 UI 输入不冲突。

## 16. 分阶段路线与完成门槛

| 阶段 | 工作内容 | 进入下一阶段前的条件 |
| --- | --- | --- |
| P0 环境与骨架 | 核对版本、主场景、设置、地图合同、卸载测试 | 工程可启动；单地图规则可验证 |
| P1 第一张成品地图 | M01 构图、模块、美术、静态烘焙、摄影机、基本截图 | 三个主构图通过；无缺失资源 |
| P2 低负担运行 | 经济/均衡档、性能测量、显存与卸载检查、Windows 导出 | 真实报告；已知问题清楚；可本地使用 |
| P3 地图库扩展 | 一次制作一张新地图，复用共享资产 | 每张新地图独立满足性能与卸载合同 |
| P4 摄影工作台 | 镜头预设、路径编辑、离线视频输出 | 同一镜头可重复输出 |
| P5 短剧工具 | 演员、动作、台词与表演快照 | 明确的新需求与独立验收 |

`chapter1.md` 覆盖 P0、P1 和 P2 的第一轮交付。它可以跨多天完成，不要求单个 6 小时窗口完成全部内容。

## 17. 明确禁止的实现捷径

- 用一张城市图片、全景贴图或远景视频冒充可观察的 3D 主体城市。
- 用几个默认立方体、随机发光窗口加 Bloom 宣布“精美赛博城市”完成。
- 把全城所有地图同时实例化后隐藏，以此声称按地图加载。
- 每次启动重新生成全城、重新展开 UV 或重新烘焙照明。
- 通过每帧扫描全部场景节点实现地图管理、距离剔除或质量切换。
- 默认解除帧率限制，让静态场景持续占满 GPU。
- 加入用户暂时不需要的 NPC、剧情或玩法，挤占第一张地图的完成时间。
- 为通过检查删除测试、缩小测试范围、编造截图、编造 profiler 数字。

## 18. 官方参考与版本边界

以下资料在设计阶段查阅；`stable` 文档可能以后更新，具体 API 以用户本机 4.7.2 的内置类文档和实际运行验证为准。

- [Godot 4.7.2 发行页](https://godotengine.org/download/archive/4.7.2-stable/)
- [渲染器及特性比较](https://docs.godotengine.org/en/stable/tutorials/rendering/renderers.html)
- [LightmapGI](https://docs.godotengine.org/en/stable/tutorials/3d/global_illumination/using_lightmap_gi.html)
- [背景资源加载](https://docs.godotengine.org/en/stable/tutorials/io/background_loading.html)
- [3D 性能优化](https://docs.godotengine.org/en/stable/tutorials/performance/optimizing_3d_performance.html)
- [遮挡剔除](https://docs.godotengine.org/en/stable/tutorials/3d/occlusion_culling.html)
- [MultiMesh](https://docs.godotengine.org/en/stable/tutorials/performance/using_multimesh.html)
- [反射探针](https://docs.godotengine.org/en/stable/tutorials/3d/global_illumination/reflection_probes.html)
- [Performance 指标](https://docs.godotengine.org/en/stable/classes/class_performance.html)
- [Movie Maker](https://docs.godotengine.org/en/stable/tutorials/animation/creating_movies.html)

本文中的地图尺寸、美术规则、技术组合、预算和工作流程属于本项目的设计决策，尚未构成对真实工程的性能测量结果。
