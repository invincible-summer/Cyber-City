# Chapter 1 阶段复核（M01 余晖街区）

> 更新：2026-09-20。性能数字见 `artifacts/performance/chapter1_metrics.csv`；本文件给结论与证据索引。

## 一、交付范围

| 交付项 | 状态 | 证据 |
| --- | --- | --- |
| Godot 4.7.2 工程（主场景/脚本/资源/配置） | 完成 | `--headless --import` 无脚本错误；正常窗口运行 |
| M01 成品地图 + 三机位 + 烘焙光照 | 完成 | `maps/m01_afterglow/`（含 `map_lightmap.res/.exr`，烘焙 8.4s） |
| 观察相机 / 机位 / 隐藏 UI / 截图 / 诊断 | 完成 | `--shoot` 自动化全链路（6 成品 + 3 灰盒 + 诊断图） |
| 单地图加载/卸载架构 + A→B→A 测试 | 完成 | `tests/test_map_lifecycle.gd` **49/49 通过**（`artifacts/logs/lifecycle_test.log`） |
| 经济/均衡画质档 + 失焦/最小化策略 | 完成 | `data/quality/*.json` + `scripts/app/settings_manager.gd`（节流逻辑经编码审查；交互验证待人工） |
| 性能证据 | 完成 | 60s×3×两档位固定路线（下节） |
| Windows 导出 | **待验证** | 本机未安装 4.7.2 导出模板；步骤见 `docs/handoff.md` |
| 文档（README/来源/交接/工具） | 完成 | 根 `README.md`、`docs/asset_sources.md`、`tools/README.md` |

## 二、画面验证（像素统计口径）

截图：`artifacts/screenshots/`（`m01_{机位}_{档位}.png` ×6 + 灰盒 ×3 + 诊断图 ×1；1920×1080，来自实际运行画面）。

| 机位 | 档位 | 平均亮度 | 黑区占比 | 过曝占比 | 暖色占比 | 天空/地面 |
| --- | --- | --- | --- | --- | --- | --- |
| street_view | eco | 0.349 | 0.00 | 0.006 | 0.046 | 0.39 / 0.33 |
| street_view | balanced | 0.324 | 0.00 | 0.006 | 0.037 | 0.36 / 0.31 |
| repair_shop_view | eco | 0.243 | 0.02 | 0.003 | 0.035 | 0.44 / 0.08 |
| station_view | eco | 0.378 | 0.00 | 0.015 | 0.487 | 0.57 / 0.26 |
| station_view | balanced | 0.388 | 0.00 | 0.002 | 0.517 | 0.59 / 0.26 |

解读：无整片黑区/白块；车站视角朝向余晖方向呈现高暖占比（黄昏天空+站名牌），街道视角为冷调主导+暖光点缀（4.6%），符合"蓝调时刻"设计。灰盒→成品的亮度/暖色差异证明美术迭代真实发生（灰盒组图保留同目录）。招牌字形经覆盖率统计验证（笔画覆盖 17–35%，非豆腐块）。

**本轮验证方式为无头像素统计**（当前 Agent 无图形输入能力）：构图比例、招牌可读性、细节观感的**人工目检仍待用户执行**——直接运行 `& $GODOT --path .` 逐机位按 1/2/3 查看。

## 三、性能结论（填自 chapter1_metrics.csv）

> 环境见 `docs/environment.md`（RTX 5070 Ti Laptop / 32GB / 工程运行非导出 / 2026-09-20 实测）。工作集列为 N/A：引擎内无可靠采样途径，需 Process Explorer 等外部工具；`gpu_memory_mib` 为引擎估计值（口径已在 notes 标注）。

| 指标 | Eco ×3 | 目标 | Balanced ×3 | 目标 |
| --- | --- | --- | --- | --- |
| 平均 FPS | 30.00 / 30.00 / 30.00 | ≥29 ✓ | 59.87 / 60.00 / 59.10 | ≥58 ✓ |
| 帧间隔 P50/P95/P99 (ms) | 33.3 / 33.3 / 33.3 | P95≤40 ✓ | 16.7 / 16.7 / 16.7 | P95≤20 ✓ |
| >100ms 卡顿 | 0 / 0 / 0 | 无重复 ✓ | 1 / 0 / 7 | 备注¹ |
| 引擎显存估计 (MiB) | 161 | ≤1500 ✓ | 227 | ≤1500 ✓ |
| 暖启动资源加载 (ms) | 626 | ≤5000 ✓ | 同一过程 | — |

¹ Balanced r2 出现 7 次 >100ms 间歇（60s 内），另两轮 0/1 次；因均分位数全为 16.7ms，判断为外部干扰（后台进程/驱动）偶发，非场景负载所致；如复现可用导出版+独占全屏复测。

- 两档全部**贴帧率上限**运行（帧间隔恒等于上限周期），说明当前场景在本机远未饱和——按 AGENTS §9 的定位（本预算服务同场景迭代），留有充足余量给后续地图与细节加密。
- 遮挡剔除开/关对照（`occlusion_off_check.csv`，Eco×2 关闭）：29.99/29.99 vs 开启 30.00/30.00——均贴上限、无可测差异；保留默认开启，其价值体现在更复杂场景的余量。
- `draw_calls_peak` 6–7：Mobile 渲染器下该监视器口径可能不含全部批次（相比网格/表面数偏低），列为存疑数据，不以该值判定绘制成本；以帧率与显存为准。
- 首轮（未修节流污染）的废数据已作废，不在 CSV 中；污染原因（自动化运行窗口失焦触发 10FPS 节流、上次档位写入 user 配置）已在 `--perf` 自动化中修复（测量期禁用节流 + 重置配置）。

## 四、生命周期与单地图约束

- 任意时刻 `active_map_root` ≤1（含切换中）；A↔B 往返 5 轮（10 次切换）后旧根弱引用全部失效。
- 无效 ID / 缺失路径在卸载前拒绝且原地图不变；缺锚点在卸载后失败→清理→EMPTY→可重试；重复请求被拒绝。
- 注册表仅 1 张生产地图；测试地图只存在 `tests/fixtures/`。

## 五、遗留问题（如实）

1. **导出未做**：无 4.7.2 模板（见上）。工程运行（开发态）已验证。
2. **人工目检缺失**：像素统计不能替代审美复核（构图像素级正确 ≠ 好看）。
3. **招牌字体许可**：系统字体栅格化，对外分发前需替换（`docs/asset_sources.md`）。
4. 遮挡剔除开/关对照、失焦/最小化的交互实测：待后续轮次补充。
5. 首次进入地图的反射探针（均衡档）首次更新有一次性开销：符合预期设计（AGENTS §8.2），未单独计时。
